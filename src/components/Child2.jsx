import styled from "styled-components";

export const Child2 = () => {

    return (
        <>
            <SButton>解答</SButton>
        </>
    );
};

const SButton = styled.button`
    margin: 10px;
    font-size: 15px;
`